;;; abstraction of rectangle

(load "2.scm")

; use two lines to define a rectangle
(define (make-rectangle s1 s2)
    (cond ()))

; swap two points of the line
(define (swap-two-points s)
    (cons (end-segment s) (start-segment s)))